[package]
name = "yxt"
version = "0.1.0"
edition = "2024"

[dependencies]
libc = "0.2"
rustix = { version = "1", features = [ "event", "fs", "net", "pipe", "process", "pty", "stdio", "termios", "thread" ] }
rustix-openpty = "0.2"

[dev-dependencies]
tempfile = "3"
